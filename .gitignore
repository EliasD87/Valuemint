# Dependencies
node_modules/

# Build output
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# Secrets. PINATA_JWT, FILEBASE_SECRET and the rest live in .env.local and must
# never reach a remote. On Vercel these are set in Project Settings instead.
.env
.env.*
!.env.example

# Deploy tooling
.vercel/

# Editors / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# Project notes for Claude. Local only.
#
# This is a working document: it records the security posture, the traps already
# paid for, and the weaknesses not yet closed. That last part is the problem —
# a public copy tells anyone exactly where the soft spots are. It carries no
# credentials, but it should never have been pushed.
CLAUDE.md
